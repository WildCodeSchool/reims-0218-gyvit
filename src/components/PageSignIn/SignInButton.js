import React from "react"
import { Button } from "reactstrap"
import "./styles-page-sign-in/signInButton.css"

const SignInButton = () => (
  <div>
    <Button className="buttonSignIn" color="info">
      Se connecter
    </Button>
  </div>
)

export default SignInButton
