//TODO CREATE SIGN IN TITLE
import React from "react"
import { NavLink } from "reactstrap"

const SignInGetStarted = () => (
  <div>
    <p>
      Don't have an account?<NavLink href="#"> Get started </NavLink>
    </p>
  </div>
)

export default SignInGetStarted
