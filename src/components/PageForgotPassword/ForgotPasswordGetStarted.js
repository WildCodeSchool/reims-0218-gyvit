import React from "react"

const ForgotPasswordGetStarted = () => (
  <div style={{ paddingTop: "20%" }}>
    <p>Pas de compte ? Commencer dès maintennant !</p>
  </div>
)

export default ForgotPasswordGetStarted
